/**
 * Interface that represents the contact form data.
 * @param name - The name of the contact.
 * @param email - The email of the contact.
 * @param message - The message of the contact.
 */
export interface FormDataProps {
    name: string;
    email: string;
    message: string;
}
