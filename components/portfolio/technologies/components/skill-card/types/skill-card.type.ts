import { SkillItemProps } from "@/components/portfolio/technologies/types";

/**
 * Type that represents the props of a skill card.
 * @param skill - The skill to display.
 */
export interface SkillCardProps {
    skill: SkillItemProps;
}
